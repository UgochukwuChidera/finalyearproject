OPENROUTER_API_KEY="your-api-key-here"
# Optional: override the vision model used for extraction.
# Default is openai/gpt-4o (supports logprobs, stable production model).
# OPENROUTER_MODEL="openai/gpt-4o"
